[unix_http_server]
file = /.init/supervisor.sock

[supervisord]
logfile = /dev/stdout
loglevel = info
pidfile = /.init/supervisord.pid
identifier = supervisor

[include]
files = /.init/services/run/*.conf

[supervisorctl]
serverurl = unix:///.init/supervisor.sock
